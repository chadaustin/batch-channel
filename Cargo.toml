[package]
name = "batch-channel"
version = "0.1.0"
edition = "2021"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[dev-dependencies]
anyhow = "1"
async-std = "1"
async-trait = "0.1"
criterion = "0.5"
crossbeam = "0.8"
futures = "0.3"

[[bench]]
name = "async"
harness = false
